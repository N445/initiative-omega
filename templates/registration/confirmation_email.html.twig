{% extends 'base-email.html.twig' %}

{% block body %}
    <container class="header">
        <row>
            <columns>
                <h1 class="text-center">Bienvenue chez la Shadow of Caliban</h1>
                <p>
                    Avant toute chose vous devez valider votre email en cliquant sur le lien ci dessous
                    <br>
                    <br>
                    <a href="{{ signedUrl }}">Confirmer mon e-mail</a>.
                    Ce lien expirera dans {{ expiresAtMessageKey|trans(expiresAtMessageData, 'VerifyEmailBundle') }}.
                </p>
            </columns>
        </row>
    </container>
{% endblock %}
