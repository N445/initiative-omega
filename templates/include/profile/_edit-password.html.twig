{{ form_start(formEditPassword) }}
<table class="table table-dark table-sm">
    <tbody>
    <tr>
        <th>{{ form_label(formEditPassword.password.first) }}</th>
        <td>
            {{ form_widget(formEditPassword.password.first) }}
            {{ form_errors(formEditPassword.password.first) }}
        </td>
    </tr>
    <tr>
        <th>{{ form_label(formEditPassword.password.second) }}</th>
        <td>
            {{ form_widget(formEditPassword.password.second) }}
            {{ form_errors(formEditPassword.password.second) }}
        </td>
    </tr>
    </tbody>
    <tfooter>
        <tr>
            <td colspan="100%">
                <button type="submit" class="btn btn-sm btn-primary">
                    Modifier
                </button>
            </td>
        </tr>
    </tfooter>
</table>
{{ form_end(formEditPassword) }}
