{% extends '@EasyAdmin/page/content.html.twig' %}

{% block page_title %}
    Dashboard
{% endblock %}

{% block page_content %}
    <div class="row g-3">
        <div class="col-md-6">
            {% if charts.memberChart is not null %}
                <div class="card text-bg-dark">
                    <div class="card-header">
                        <h2 class="mb-0">Xp des membres</h2>
                    </div>
                    <div class="card-body">
                        {{ render_chart(charts.memberChart) }}
                    </div>
                </div>
            {% endif %}
            <div class="card text-bg-dark mt-3">
                <div class="card-header">
                    <h2 class="mb-0">Récap de la flotte</h2>
                </div>
                <div class="card-body">
                    {{ render_chart(charts.shipsChart) }}
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card text-bg-dark">
                <div class="card-header">
                    <h2 class="mb-0">Events</h2>
                </div>
                <div class="card-body">
                    <div id='calendar'></div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
