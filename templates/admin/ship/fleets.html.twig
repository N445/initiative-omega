{% extends '@EasyAdmin/page/content.html.twig' %}

{% block page_title %}
    Flotte de la corpo
{% endblock %}


{% block page_content %}

    <table class="table datagrid">
        <thead>
        {% block table_head %}
            <tr>
                <th class="text-left">
                    <span>Nom du vaisseau</span>
                </th>
                <th class="text-left">
                    <span>Nb in corpo</span>
                </th>
                <th></th>
            </tr>
        {% endblock table_head %}
        </thead>
        <tbody>
        {% block table_body %}
            {% for ship in ships %}
                <tr data-id="{{ ship.id }}">
                    <td>{{ ship.name }}</td>
                    <td>{{ ship.nbTotalInFleets }}</td>
                    {% block entity_actions %}
                        <td class="actions">
                            <div class="dropdown dropdown-actions">
                                <a class="dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                                   aria-haspopup="true" aria-expanded="false">
                                    {# don't use FontAwesome 'fa-ellipsis-h' icon here because it doesn't look good #}
                                    {# this icon is 'dots-horizontal' icon from https://heroicons.com/ #}
                                    <svg xmlns="http://www.w3.org/2000/svg" height="21" width="21" fill="none"
                                         viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                              d="M5 12h.01M12 12h.01M19 12h.01M6 12a1 1 0 11-2 0 1 1 0 012 0zm7 0a1 1 0 11-2 0 1 1 0 012 0zm7 0a1 1 0 11-2 0 1 1 0 012 0z"/>
                                    </svg>
                                </a>
                                <div class="dropdown-menu dropdown-menu-right">
                                    {% for action in actions %}
{#                                        <a class="dropdown-item'"#}
{#                                           href="{{ action.linkUrl }}"#}
{#                                        {% for name, value in action.htmlAttributes %}{{ name }}="{{ (value.trans is defined ? value|trans : value)|e('html_attr') }}" {% endfor %}>#}
{#                                        {%- if action.icon %}<i class="action-icon {{ action.icon }}"></i> {% endif -%}#}
{#                                        {%- if action.label is not empty -%}<span#}
{#                                                class="action-label">{{ action.label|trans|raw }}</span>{%- endif -%}#}
{#                                        </a>#}
                                    {% endfor %}


                                    {#                                    <a class="dropdown-item" #}
                                    {#                                       href="{{ path('ADMIN_SHIP_FLEETS', {'idShip': ship.id}) }}"> #}
                                    {#                                                <span class="action-label"> #}
                                    {#                                                Consulter #}
                                    {#                                                </span> #}
                                    {#                                    </a> #}
                                </div>
                            </div>
                        </td>
                    {% endblock entity_actions %}
                </tr>
            {% endfor %}
        {% endblock table_body %}
        </tbody>
        <tfoot>
        </tfoot>
    </table>
{% endblock %}
